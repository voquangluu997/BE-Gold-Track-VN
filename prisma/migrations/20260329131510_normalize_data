-- prisma/migrations/20240329_normalize_data/migration.sql

-- Chuẩn hóa dữ liệu gold_prices
UPDATE gold_prices SET brand = UPPER(brand);
UPDATE gold_prices SET city = UPPER(city);
UPDATE gold_prices SET type = UPPER(type);

-- Chuẩn hóa dữ liệu exchange_rates
UPDATE exchange_rates SET currency = UPPER(currency);
UPDATE exchange_rates SET bank = UPPER(bank);